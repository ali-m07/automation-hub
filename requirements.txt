# Core dependencies (required for all platforms)
pandas>=2.0.0
openpyxl>=3.1.0

# Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
jinja2>=3.1.2
aiofiles>=23.2.1

# Security / auth
passlib>=1.7.4
pyotp>=2.9.0
itsdangerous>=2.1.0

# PSD Processing (No Photoshop required!)
psd-tools==1.10.9
Pillow==11.3.0
numpy>=1.24.0

# Database connectors (Excel sync to SQL Server)
# Optional: install pyodbc; on Linux install ODBC driver for SQL Server separately
pyodbc>=4.0.39

# Tables storage (Postgres) - optional but enabled when POSTGRES_DSN is set
psycopg[binary]>=3.2.0

# Optional: Redis (caching / rate limiting / session store)
# Install when using REDIS_URL
redis>=5.0.0

# Optional: Celery for distributed task queue
# Install when using CELERY_BROKER_URL
celery>=5.3.0

# Cloud connectors (Google Sheets, Airtable, Notion)
requests>=2.31.0

# Configuration management
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
httpx>=0.25.0  # For testing FastAPI endpoints

# Email functionality
# (smtplib is built-in, no extra package needed)

# Windows-only dependency (for Photoshop COM automation - OLD DESKTOP APP)
# Install only on Windows: pip install comtypes
# comtypes>=1.4.0
